# Generated by Django 3.1.2 on 2020-11-02 11:33

from django.db import migrations


def capitalize_student_name(apps, schema_editor):
    # We can't import the Person model directly as it may be a newer
    # version than this migration expects. We use the historical version.
    student_temp = apps.get_model('core', 'Student')
    for student in student_temp.objects.all():
        student.firstname = student.firstname.capitalize()
        student.lastname = student.lastname.capitalize()
        student.save()


def student_mobile(apps, schema_editor):
    # We can't import the Person model directly as it may be a newer
    # version than this migration expects. We use the historical version.
    student_temp = apps.get_model('core', 'Student')
    for student in student_temp.objects.all():
        if student.phone is None:
            student.phone = 'No number'
        student.save()


def capitalize_teacher_name(apps, schema_editor):
    # We can't import the Person model directly as it may be a newer
    # version than this migration expects. We use the historical version.
    teacher_temp = apps.get_model('core', 'Teacher')
    for teacher in teacher_temp.objects.all():
        teacher.firstname = teacher.firstname.capitalize()
        teacher.lastname = teacher.lastname.capitalize()
        teacher.save()


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0005_student_phone'),
    ]

    operations = [
        migrations.RunPython(capitalize_student_name),
        migrations.RunPython(student_mobile),
        migrations.RunPython(capitalize_teacher_name)
    ]
